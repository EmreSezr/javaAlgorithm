package org.emresezer.soru6;

import java.util.Scanner;

public class WordLength {
    // Bir metindeki harf sayısını bulan bir algori tma yazın.
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Cümle veya kelime yazınız(Boşluklar dahildir) :  ");
        String cumle = scanner.nextLine();
        char[] ch = new char[cumle.length()];
        int kelimeSayac = 0;
        //if(ch.is)
        System.out.println("Karakter toplamı : " + ch.length);

    }
}
